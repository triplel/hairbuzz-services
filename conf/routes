# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
#GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                 controllers.Assets.at(path="/public", file)
GET         /                             controllers.HairBuzzStub.index


POST        /search/venue                 controllers.SearchController.searchVenue
POST        /search/stylist               controllers.SearchController.searchStylist

# Stub services
GET         /stub/users/:hbid             controllers.HairBuzzStub.stubGetUserByID(hbid)
POST        /stub/search/stylist          controllers.HairBuzzStub.stubSearchHairStylists


